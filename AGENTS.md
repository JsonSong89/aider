### 我在开发 aider 的二开版本

针对 aider 的个性化修改，按下列约定记录，方便随时和官方代码对比。

## 分支约定

1. **`main` 必须和官方保持一致**。不要在 `main` 上做二开改动、提交或合并个性化功能。同步官方时只更新 `main`。
2. **所有二开修改只在 `desk` 分支进行**（从当前官方对齐后的 `main` 分出或变基）。
3. 不要把 `desk` 的二开提交回灌到 `main`。

## 功能 diff 文档约定

1. **`功能diff导航.md`**：只列功能简述（表格/短列表），并链接到详细文档。不要在此写实现细节。
2. **`.diff_docs/`**：每个功能点一个独立 Markdown 文件，写完整说明（背景、改动位置、行为、范围、示例、风险等）。这就是二开的 diff 说明。
3. **在 `desk` 上新增或修改功能时**：
   - 代码改动只提交到 `desk`；
   - 在 `.diff_docs/` 新增或更新对应功能文件；
   - 同步在 `功能diff导航.md` 增加/更新一行简述与链接；
   - 文件名用简短中文或英文短语标明功能点即可。
4. 旧名 `功能diff说明.md` 已废弃，统一使用上述结构。
